---
phase: 6
plan: 1
wave: 1
depends_on: [5]
files_modified:
  - "src/components/PdfPreviewModal.tsx"
  - "src/components/Header.tsx"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Preview modal primary action is functionally correct — blob fetch, iframe display, download, and open-in-new-tab all work"
    - "Navigation link order is: Home → Papers/PPTs → About → Bulk Download → Upload"
  artifacts: []
---

# Plan 6.1: Fix Preview Modal Action + Reorder Navbar

<objective>
Two UI polish fixes. First, the modal footer currently shows "Open in new tab" before "Download" — the download action should be primary (rightmost/most prominent). Second, the navbar currently orders links: Home → Papers → Bulk Download → About → (PPT: Upload). The desired order is: Home → Papers → About → Bulk Download → Upload (logical: browse first, then info, then actions).

Purpose: Make the UI more intuitive — download is the main action, and nav ordering should match how users naturally flow through the app.
Output: Modified `PdfPreviewModal.tsx` (swap button order, ensure download is primary CTA) and `Header.tsx` (reorder nav links).
</objective>

<context>
Load for context:
- src/components/PdfPreviewModal.tsx
- src/components/Header.tsx
- src/components/PaperCard.tsx
- src/pages/Papers.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix PdfPreviewModal footer button order</name>
  <files>src/components/PdfPreviewModal.tsx</files>
  <action>
    In `PdfPreviewModal.tsx`, the `<DialogFooter>` currently renders buttons as:
    1. "Open in new tab" (outline, secondary)
    2. "Download" (primary, filled)

    This order is already correct visually (Download is the right/primary CTA). However, confirm the `handleOpenInNewTab` function actually opens the `pdfPath` (the original raw URL), NOT a blob:// URL. Current code at line 95-99 opens `pdfPath` directly — this is intentional and correct (opens in GitHub raw viewer).

    The actual fix needed: the modal description (line 106-108) duplicates the path as the DialogDescription — this is noisy. Replace `{pdfPath}` in the DialogDescription with a user-friendly label like `"PDF Document"` or remove DialgDescription entirely (set it to an empty string or descriptive text like `"Review and download your document below."`).

    Also: the footer `<small>` at line 129-131 shows the full raw URL which is distractingly long — truncate it more aggressively or remove it entirely from the footer. The title in the dialog header already identifies the document.

    DO NOT change the download/open-in-tab functionality — it works.
    DO NOT change the blob fetch logic — it works.
    ONLY change the descriptive/metadata text visible to users.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>
    - `npm run build` exits with code 0
    - DialogDescription no longer shows the raw URL as its primary content
    - Footer no longer shows a duplicate raw URL truncated text (or it's hidden)
  </done>
</task>

<task type="auto">
  <name>Reorder Header navigation links</name>
  <files>src/components/Header.tsx</files>
  <action>
    In `Header.tsx`, the nav section (lines 45-96) currently has this link order:
    1. Home
    2. Papers/PPTs (search)
    3. Bulk Download (with icon)
    4. About (with icon)
    5. Upload (PPT mode only, with icon)

    Change the order to:
    1. Home
    2. Papers/PPTs (search)
    3. About (with Info icon)
    4. Bulk Download (with Download icon)
    5. Upload (PPT mode only, with Upload icon)

    This groups "informational" pages (Home → Papers → About) before "action" pages (Bulk Download → Upload), which is a more conventional nav ordering.

    Do this by simply moving the About `<Link>` block (currently at lines 74-83) to appear BEFORE the Bulk Download `<Link>` block (currently at lines 64-73).

    No styling changes needed — just reorder the JSX blocks.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>
    - `npm run build` exits with code 0
    - In the rendered header, nav order is: Home → Papers/PPTs → About → Bulk → Upload
  </done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `npm run build` succeeds with no TypeScript errors
- [ ] Nav order in Header is: Home, Papers/PPTs, About, Bulk Download, (Upload in PPT mode)
- [ ] PDFPreviewModal no longer shows redundant raw URL text in description/footer
</verification>

<success_criteria>
- [ ] Build passes
- [ ] Modal dialog description is user-friendly, not a raw GitHub URL
- [ ] Navbar order matches: Home → Papers → About → Bulk → Upload
</success_criteria>
