---
phase: 6
plan: 2
wave: 2
depends_on: [6.1]
files_modified:
  - "src/types/examPaper.ts"
  - "src/components/PaperCard.tsx"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "PaperCard shows a faculty_name badge if the data field exists, and gracefully skips it if not present"
    - "No TypeScript build errors are introduced"
  artifacts: []
---

# Plan 6.2: Add Faculty Name Metadata Display

<objective>
The current `DisplayPaper` and `AssetMappingItem` types do not have a `faculty_name` or `teacher_name` field. The asset_mapping.json data may include a `faculty_name` field per entry (or it may be absent for older records). This plan adds optional `faculty_name` typing support and displays it in PaperCard as a badge when present.

Purpose: Show who set the exam paper or prepared the material — useful context for students.
Output: Type-safe `faculty_name?: string` in `AssetMappingItem` and `DisplayPaper`, and conditional badge rendering in `PaperCard`.
</objective>

<context>
Load for context:
- src/types/examPaper.ts
- src/components/PaperCard.tsx
- public/asset_mapping.json (check if faculty_name field already exists in any entries — if it doesn't exist in the JSON at all yet, note that in a comment and the badge will simply never render until the data is populated)
</context>

<tasks>

<task type="auto">
  <name>Add faculty_name to type definitions</name>
  <files>src/types/examPaper.ts</files>
  <action>
    In `src/types/examPaper.ts`:

    1. In `AssetMappingItem` interface (lines 16-23), add:
       ```ts
       faculty_name?: string;
       ```

    2. In `DisplayPaper` interface (lines 61-74), add:
       ```ts
       faculty_name?: string;
       ```

    3. In `NormalizedPaper` interface (lines 29-42), add:
       ```ts
       faculty_name?: string;
       ```

    These are all optional fields — no existing code breaks.
    DO NOT remove or rename any existing fields.
  </action>
  <verify>npm run build 2>&1 | grep -c "error TS" || true</verify>
  <done>
    - `npm run build` passes with 0 TypeScript errors
    - `grep "faculty_name" src/types/examPaper.ts` returns 3 lines
  </done>
</task>

<task type="auto">
  <name>Display faculty_name badge in PaperCard</name>
  <files>src/components/PaperCard.tsx</files>
  <action>
    In `src/components/PaperCard.tsx`, inside the badges section (lines 112-146), add a conditional badge for `faculty_name` after the existing `branch` badge and before the `type` badge:

    ```tsx
    {paper.faculty_name && (
      <Badge variant="outline" className="text-xs">
        {paper.faculty_name}
      </Badge>
    )}
    ```

    The `paper` prop is of type `DisplayPaper` which will now have `faculty_name?: string` after the type update.

    Also check: the `useExamPapers` hook or equivalent data transformer may need to pass through `faculty_name` from the raw asset mapping data. Check `src/hooks/useExamPapers.ts` or the data normalizer — if there is a mapping/transform step that builds `DisplayPaper` objects, ensure `faculty_name` is copied from the source. If the normalizer constructs `DisplayPaper` from `NormalizedPaper`, add `faculty_name: paper.faculty_name` to that mapping.

    If `public/asset_mapping.json` has no `faculty_name` fields yet, the badge will simply never render and that is acceptable — future data updates will populate it.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>
    - `npm run build` exits with code 0
    - `grep "faculty_name" src/components/PaperCard.tsx` returns at least one line
    - Badge renders when `faculty_name` string is present, absent when undefined
  </done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `npm run build` succeeds with no TypeScript errors
- [ ] `faculty_name` is optional in `AssetMappingItem`, `NormalizedPaper`, and `DisplayPaper`
- [ ] PaperCard conditionally renders a `faculty_name` badge
- [ ] No existing functionality is broken
</verification>

<success_criteria>
- [ ] Build passes (0 TS errors)
- [ ] Types updated with optional faculty_name field
- [ ] PaperCard renders faculty name badge when data is available
</success_criteria>
